---
'vue-helios-kanban-companion': patch
---

Module-aware hover selection: in targeting mode (hold Alt/Option, or toggle via the floating HK button) the highlight now snaps to the root element of the Vue component under the cursor instead of the raw DOM node, so moving the mouse highlights whole modules.
